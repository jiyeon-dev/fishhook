# CLAUDE.md

이 저장소의 작업 규칙은 [AGENTS.md](AGENTS.md)에 있다. **먼저 읽는다.**

특히 동작을 바꾸는 작업은 아래 셋을 모두 끝내야 완료다. 사용자가 따로 요청하지
않아도 기본으로 수행한다.

1. **문서 업데이트** — 바뀐 동작을 `docs/`의 해당 문서와 수동 검증 체크리스트에 반영
2. **버전 업그레이드** — `manifest.json`의 `version` 상향 (`package.json` 없음)
3. **테스트** — `node --test` 통과, 로직 변경분에 테스트 추가

상세 규칙(버전 자리 선택 기준, i18n 함정, 보안 기본값, 메시지 계약)은 AGENTS.md 참고.
